name: test
concurrency:
  group: ${{ github.head_ref }}-${{ github.workflow}}
  cancel-in-progress: true

on:
  pull_request:
    branches: [ main ]

jobs:
  changes:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      components-ft-concept-button: ${{ steps.filter.outputs.components-ft-concept-button }}
      components-g-audio: ${{ steps.filter.outputs.components-g-audio }}
      components-n-notification: ${{ steps.filter.outputs.components-n-notification }}
      components-o-audio: ${{ steps.filter.outputs.components-o-audio }}
      components-o-autocomplete: ${{ steps.filter.outputs.components-o-autocomplete }}
      components-o-banner: ${{ steps.filter.outputs.components-o-banner }}
      components-o-big-number: ${{ steps.filter.outputs.components-o-big-number }}
      components-o-buttons: ${{ steps.filter.outputs.components-o-buttons }}
      components-o-colors: ${{ steps.filter.outputs.components-o-colors }}
      components-o-comments: ${{ steps.filter.outputs.components-o-comments }}
      components-o-cookie-message: ${{ steps.filter.outputs.components-o-cookie-message }}
      components-o-date: ${{ steps.filter.outputs.components-o-date }}
      components-o-editorial-layout: ${{ steps.filter.outputs.components-o-editorial-layout }}
      components-o-editorial-typography: ${{ steps.filter.outputs.components-o-editorial-typography }}
      components-o-expander: ${{ steps.filter.outputs.components-o-expander }}
      components-o-fonts: ${{ steps.filter.outputs.components-o-fonts }}
      components-o-footer: ${{ steps.filter.outputs.components-o-footer }}
      components-o-footer-services: ${{ steps.filter.outputs.components-o-footer-services }}
      components-o-forms: ${{ steps.filter.outputs.components-o-forms }}
      components-o-ft-affiliate-ribbon: ${{ steps.filter.outputs.components-o-ft-affiliate-ribbon }}
      components-o-grid: ${{ steps.filter.outputs.components-o-grid }}
      components-o-header: ${{ steps.filter.outputs.components-o-header }}
      components-o-header-services: ${{ steps.filter.outputs.components-o-header-services }}
      components-o-icons: ${{ steps.filter.outputs.components-o-icons }}
      components-o-labels: ${{ steps.filter.outputs.components-o-labels }}
      components-o-layout: ${{ steps.filter.outputs.components-o-layout }}
      components-o-lazy-load: ${{ steps.filter.outputs.components-o-lazy-load }}
      components-o-loading: ${{ steps.filter.outputs.components-o-loading }}
      components-o-message: ${{ steps.filter.outputs.components-o-message }}
      components-o-meter: ${{ steps.filter.outputs.components-o-meter }}
      components-o-multi-select: ${{ steps.filter.outputs.components-o-multi-select }}
      components-o-normalise: ${{ steps.filter.outputs.components-o-normalise }}
      components-o-overlay: ${{ steps.filter.outputs.components-o-overlay }}
      components-o-quote: ${{ steps.filter.outputs.components-o-quote }}
      components-o-share: ${{ steps.filter.outputs.components-o-share }}
      components-o-social-follow: ${{ steps.filter.outputs.components-o-social-follow }}
      components-o-spacing: ${{ steps.filter.outputs.components-o-spacing }}
      components-o-stepped-progress: ${{ steps.filter.outputs.components-o-stepped-progress }}
      components-o-subs-card: ${{ steps.filter.outputs.components-o-subs-card }}
      components-o-syntax-highlight: ${{ steps.filter.outputs.components-o-syntax-highlight }}
      components-o-table: ${{ steps.filter.outputs.components-o-table }}
      components-o-tabs: ${{ steps.filter.outputs.components-o-tabs }}
      components-o-teaser: ${{ steps.filter.outputs.components-o-teaser }}
      components-o-teaser-collection: ${{ steps.filter.outputs.components-o-teaser-collection }}
      components-o-toggle: ${{ steps.filter.outputs.components-o-toggle }}
      components-o-tooltip: ${{ steps.filter.outputs.components-o-tooltip }}
      components-o-top-banner: ${{ steps.filter.outputs.components-o-top-banner }}
      components-o-topper: ${{ steps.filter.outputs.components-o-topper }}
      components-o-typography: ${{ steps.filter.outputs.components-o-typography }}
      components-o-video: ${{ steps.filter.outputs.components-o-video }}
      components-o-viewport: ${{ steps.filter.outputs.components-o-viewport }}
      components-o-visual-effects: ${{ steps.filter.outputs.components-o-visual-effects }}
      libraries-ftdomdelegate: ${{ steps.filter.outputs.libraries-ftdomdelegate }}
      libraries-math: ${{ steps.filter.outputs.libraries-math }}
      libraries-o-autoinit: ${{ steps.filter.outputs.libraries-o-autoinit }}
      libraries-o-brand: ${{ steps.filter.outputs.libraries-o-brand }}
      libraries-o-errors: ${{ steps.filter.outputs.libraries-o-errors }}
      libraries-o-tracking: ${{ steps.filter.outputs.libraries-o-tracking }}
      libraries-o-utils: ${{ steps.filter.outputs.libraries-o-utils }}
      libraries-sass-mq: ${{ steps.filter.outputs.libraries-sass-mq }}
      presets-eslint-config-origami-component: ${{ steps.filter.outputs.presets-eslint-config-origami-component }}
      presets-remark-preset-lint-origami-component: ${{ steps.filter.outputs.presets-remark-preset-lint-origami-component }}
      presets-stylelint-config-origami-component: ${{ steps.filter.outputs.presets-stylelint-config-origami-component }}
      tools-a11y: ${{ steps.filter.outputs.tools-a11y }}
      tools-create-component: ${{ steps.filter.outputs.tools-create-component }}
      tools-demo-build: ${{ steps.filter.outputs.tools-demo-build }}
      tools-origami-bower-safe-version-supervisor: ${{ steps.filter.outputs.tools-origami-bower-safe-version-supervisor }}
      tools-origami-tools-helpers: ${{ steps.filter.outputs.tools-origami-tools-helpers }}
      tools-sass-compilation: ${{ steps.filter.outputs.tools-sass-compilation }}
      tools-test-javascript: ${{ steps.filter.outputs.tools-test-javascript }}
      tools-test-sass: ${{ steps.filter.outputs.tools-test-sass }}
      tools-validate-component-demo-html: ${{ steps.filter.outputs.tools-validate-component-demo-html }}
      tools-verify-origami-json: ${{ steps.filter.outputs.tools-verify-origami-json }}
      tools-verify-package-json: ${{ steps.filter.outputs.tools-verify-package-json }}
      apps-storybook-addons-background: ${{ steps.filter.outputs.apps-storybook-addons-background }}
      apps-storybook-addons-guidelines: ${{ steps.filter.outputs.apps-storybook-addons-guidelines }}
      apps-storybook-addons-html: ${{ steps.filter.outputs.apps-storybook-addons-html }}
      apps-storybook-addons-markdown-tabs: ${{ steps.filter.outputs.apps-storybook-addons-markdown-tabs }}
      apps-storybook: ${{ steps.filter.outputs.apps-storybook }}
      apps-website: ${{ steps.filter.outputs.apps-website }}
    steps:
    - uses: dorny/paths-filter@v2
      id: filter
      with:
        filters: |
          components-ft-concept-button:
            - 'components/ft-concept-button/**'
          components-g-audio:
            - 'components/g-audio/**'
          components-n-notification:
            - 'components/n-notification/**'
          components-o-audio:
            - 'components/o-audio/**'
          components-o-autocomplete:
            - 'components/o-autocomplete/**'
          components-o-banner:
            - 'components/o-banner/**'
          components-o-big-number:
            - 'components/o-big-number/**'
          components-o-buttons:
            - 'components/o-buttons/**'
          components-o-colors:
            - 'components/o-colors/**'
          components-o-comments:
            - 'components/o-comments/**'
          components-o-cookie-message:
            - 'components/o-cookie-message/**'
          components-o-date:
            - 'components/o-date/**'
          components-o-editorial-layout:
            - 'components/o-editorial-layout/**'
          components-o-editorial-typography:
            - 'components/o-editorial-typography/**'
          components-o-expander:
            - 'components/o-expander/**'
          components-o-fonts:
            - 'components/o-fonts/**'
          components-o-footer:
            - 'components/o-footer/**'
          components-o-footer-services:
            - 'components/o-footer-services/**'
          components-o-forms:
            - 'components/o-forms/**'
          components-o-ft-affiliate-ribbon:
            - 'components/o-ft-affiliate-ribbon/**'
          components-o-grid:
            - 'components/o-grid/**'
          components-o-header:
            - 'components/o-header/**'
          components-o-header-services:
            - 'components/o-header-services/**'
          components-o-icons:
            - 'components/o-icons/**'
          components-o-labels:
            - 'components/o-labels/**'
          components-o-layout:
            - 'components/o-layout/**'
          components-o-lazy-load:
            - 'components/o-lazy-load/**'
          components-o-loading:
            - 'components/o-loading/**'
          components-o-message:
            - 'components/o-message/**'
          components-o-meter:
            - 'components/o-meter/**'
          components-o-multi-select:
            - 'components/o-multi-select/**'
          components-o-normalise:
            - 'components/o-normalise/**'
          components-o-overlay:
            - 'components/o-overlay/**'
          components-o-quote:
            - 'components/o-quote/**'
          components-o-share:
            - 'components/o-share/**'
          components-o-social-follow:
            - 'components/o-social-follow/**'
          components-o-spacing:
            - 'components/o-spacing/**'
          components-o-stepped-progress:
            - 'components/o-stepped-progress/**'
          components-o-subs-card:
            - 'components/o-subs-card/**'
          components-o-syntax-highlight:
            - 'components/o-syntax-highlight/**'
          components-o-table:
            - 'components/o-table/**'
          components-o-tabs:
            - 'components/o-tabs/**'
          components-o-teaser:
            - 'components/o-teaser/**'
          components-o-teaser-collection:
            - 'components/o-teaser-collection/**'
          components-o-toggle:
            - 'components/o-toggle/**'
          components-o-tooltip:
            - 'components/o-tooltip/**'
          components-o-top-banner:
            - 'components/o-top-banner/**'
          components-o-topper:
            - 'components/o-topper/**'
          components-o-typography:
            - 'components/o-typography/**'
          components-o-video:
            - 'components/o-video/**'
          components-o-viewport:
            - 'components/o-viewport/**'
          components-o-visual-effects:
            - 'components/o-visual-effects/**'
          libraries-ftdomdelegate:
            - 'libraries/ftdomdelegate/**'
          libraries-math:
            - 'libraries/math/**'
          libraries-o-autoinit:
            - 'libraries/o-autoinit/**'
          libraries-o-brand:
            - 'libraries/o-brand/**'
          libraries-o-errors:
            - 'libraries/o-errors/**'
          libraries-o-tracking:
            - 'libraries/o-tracking/**'
          libraries-o-utils:
            - 'libraries/o-utils/**'
          libraries-sass-mq:
            - 'libraries/sass-mq/**'
          presets-eslint-config-origami-component:
            - 'presets/eslint-config-origami-component/**'
          presets-remark-preset-lint-origami-component:
            - 'presets/remark-preset-lint-origami-component/**'
          presets-stylelint-config-origami-component:
            - 'presets/stylelint-config-origami-component/**'
          tools-a11y:
            - 'tools/a11y/**'
          tools-create-component:
            - 'tools/create-component/**'
          tools-demo-build:
            - 'tools/demo-build/**'
          tools-origami-bower-safe-version-supervisor:
            - 'tools/origami-bower-safe-version-supervisor/**'
          tools-origami-tools-helpers:
            - 'tools/origami-tools-helpers/**'
          tools-sass-compilation:
            - 'tools/sass-compilation/**'
          tools-test-javascript:
            - 'tools/test-javascript/**'
          tools-test-sass:
            - 'tools/test-sass/**'
          tools-validate-component-demo-html:
            - 'tools/validate-component-demo-html/**'
          tools-verify-origami-json:
            - 'tools/verify-origami-json/**'
          tools-verify-package-json:
            - 'tools/verify-package-json/**'
          apps-storybook-addons-background:
            - 'apps/storybook/addons/background/**'
          apps-storybook-addons-guidelines:
            - 'apps/storybook/addons/guidelines/**'
          apps-storybook-addons-html:
            - 'apps/storybook/addons/html/**'
          apps-storybook-addons-markdown-tabs:
            - 'apps/storybook/addons/markdown-tabs/**'
          apps-storybook:
            - 'apps/storybook/**'
          apps-website:
            - 'apps/website/**'
  test-components-ft-concept-button:
    needs: changes
    if: ${{ needs.changes.outputs.components-ft-concept-button == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/ft-concept-button lint --if-present
    - run: npm run -w components/ft-concept-button build --if-present
    - run: npm run -w components/ft-concept-button test --if-present
  test-components-g-audio:
    needs: changes
    if: ${{ needs.changes.outputs.components-g-audio == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/g-audio lint --if-present
    - run: npm run -w components/g-audio build --if-present
    - run: npm run -w components/g-audio test --if-present
  test-components-n-notification:
    needs: changes
    if: ${{ needs.changes.outputs.components-n-notification == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/n-notification lint --if-present
    - run: npm run -w components/n-notification build --if-present
    - run: npm run -w components/n-notification test --if-present
  test-components-o-audio:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-audio == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-audio lint --if-present
    - run: npm run -w components/o-audio build --if-present
    - run: npm run -w components/o-audio test --if-present
  test-components-o-autocomplete:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-autocomplete == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-autocomplete lint --if-present
    - run: npm run -w components/o-autocomplete build --if-present
    - run: npm run -w components/o-autocomplete test --if-present
  test-components-o-banner:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-banner == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-banner lint --if-present
    - run: npm run -w components/o-banner build --if-present
    - run: npm run -w components/o-banner test --if-present
  test-components-o-big-number:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-big-number == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-big-number lint --if-present
    - run: npm run -w components/o-big-number build --if-present
    - run: npm run -w components/o-big-number test --if-present
  test-components-o-buttons:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-buttons == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-buttons lint --if-present
    - run: npm run -w components/o-buttons build --if-present
    - run: npm run -w components/o-buttons test --if-present
  test-components-o-colors:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-colors == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-colors lint --if-present
    - run: npm run -w components/o-colors build --if-present
    - run: npm run -w components/o-colors test --if-present
  test-components-o-comments:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-comments == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-comments lint --if-present
    - run: npm run -w components/o-comments build --if-present
    - run: npm run -w components/o-comments test --if-present
  test-components-o-cookie-message:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-cookie-message == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-cookie-message lint --if-present
    - run: npm run -w components/o-cookie-message build --if-present
    - run: npm run -w components/o-cookie-message test --if-present
  test-components-o-date:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-date == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-date lint --if-present
    - run: npm run -w components/o-date build --if-present
    - run: npm run -w components/o-date test --if-present
  test-components-o-editorial-layout:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-editorial-layout == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-editorial-layout lint --if-present
    - run: npm run -w components/o-editorial-layout build --if-present
    - run: npm run -w components/o-editorial-layout test --if-present
  test-components-o-editorial-typography:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-editorial-typography == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-editorial-typography lint --if-present
    - run: npm run -w components/o-editorial-typography build --if-present
    - run: npm run -w components/o-editorial-typography test --if-present
  test-components-o-expander:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-expander == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-expander lint --if-present
    - run: npm run -w components/o-expander build --if-present
    - run: npm run -w components/o-expander test --if-present
  test-components-o-fonts:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-fonts == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-fonts lint --if-present
    - run: npm run -w components/o-fonts build --if-present
    - run: npm run -w components/o-fonts test --if-present
  test-components-o-footer:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-footer == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-footer lint --if-present
    - run: npm run -w components/o-footer build --if-present
    - run: npm run -w components/o-footer test --if-present
  test-components-o-footer-services:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-footer-services == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-footer-services lint --if-present
    - run: npm run -w components/o-footer-services build --if-present
    - run: npm run -w components/o-footer-services test --if-present
  test-components-o-forms:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-forms == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-forms lint --if-present
    - run: npm run -w components/o-forms build --if-present
    - run: npm run -w components/o-forms test --if-present
  test-components-o-ft-affiliate-ribbon:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-ft-affiliate-ribbon == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-ft-affiliate-ribbon lint --if-present
    - run: npm run -w components/o-ft-affiliate-ribbon build --if-present
    - run: npm run -w components/o-ft-affiliate-ribbon test --if-present
  test-components-o-grid:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-grid == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-grid lint --if-present
    - run: npm run -w components/o-grid build --if-present
    - run: npm run -w components/o-grid test --if-present
  test-components-o-header:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-header == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-header lint --if-present
    - run: npm run -w components/o-header build --if-present
    - run: npm run -w components/o-header test --if-present
  test-components-o-header-services:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-header-services == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-header-services lint --if-present
    - run: npm run -w components/o-header-services build --if-present
    - run: npm run -w components/o-header-services test --if-present
  test-components-o-icons:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-icons == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-icons lint --if-present
    - run: npm run -w components/o-icons build --if-present
    - run: npm run -w components/o-icons test --if-present
  test-components-o-labels:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-labels == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-labels lint --if-present
    - run: npm run -w components/o-labels build --if-present
    - run: npm run -w components/o-labels test --if-present
  test-components-o-layout:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-layout == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-layout lint --if-present
    - run: npm run -w components/o-layout build --if-present
    - run: npm run -w components/o-layout test --if-present
  test-components-o-lazy-load:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-lazy-load == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-lazy-load lint --if-present
    - run: npm run -w components/o-lazy-load build --if-present
    - run: npm run -w components/o-lazy-load test --if-present
  test-components-o-loading:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-loading == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-loading lint --if-present
    - run: npm run -w components/o-loading build --if-present
    - run: npm run -w components/o-loading test --if-present
  test-components-o-message:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-message == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-message lint --if-present
    - run: npm run -w components/o-message build --if-present
    - run: npm run -w components/o-message test --if-present
  test-components-o-meter:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-meter == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-meter lint --if-present
    - run: npm run -w components/o-meter build --if-present
    - run: npm run -w components/o-meter test --if-present
  test-components-o-multi-select:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-multi-select == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-multi-select lint --if-present
    - run: npm run -w components/o-multi-select build --if-present
    - run: npm run -w components/o-multi-select test --if-present
  test-components-o-normalise:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-normalise == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-normalise lint --if-present
    - run: npm run -w components/o-normalise build --if-present
    - run: npm run -w components/o-normalise test --if-present
  test-components-o-overlay:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-overlay == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-overlay lint --if-present
    - run: npm run -w components/o-overlay build --if-present
    - run: npm run -w components/o-overlay test --if-present
  test-components-o-quote:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-quote == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-quote lint --if-present
    - run: npm run -w components/o-quote build --if-present
    - run: npm run -w components/o-quote test --if-present
  test-components-o-share:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-share == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-share lint --if-present
    - run: npm run -w components/o-share build --if-present
    - run: npm run -w components/o-share test --if-present
  test-components-o-social-follow:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-social-follow == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-social-follow lint --if-present
    - run: npm run -w components/o-social-follow build --if-present
    - run: npm run -w components/o-social-follow test --if-present
  test-components-o-spacing:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-spacing == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-spacing lint --if-present
    - run: npm run -w components/o-spacing build --if-present
    - run: npm run -w components/o-spacing test --if-present
  test-components-o-stepped-progress:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-stepped-progress == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-stepped-progress lint --if-present
    - run: npm run -w components/o-stepped-progress build --if-present
    - run: npm run -w components/o-stepped-progress test --if-present
  test-components-o-subs-card:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-subs-card == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-subs-card lint --if-present
    - run: npm run -w components/o-subs-card build --if-present
    - run: npm run -w components/o-subs-card test --if-present
  test-components-o-syntax-highlight:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-syntax-highlight == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-syntax-highlight lint --if-present
    - run: npm run -w components/o-syntax-highlight build --if-present
    - run: npm run -w components/o-syntax-highlight test --if-present
  test-components-o-table:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-table == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-table lint --if-present
    - run: npm run -w components/o-table build --if-present
    - run: npm run -w components/o-table test --if-present
  test-components-o-tabs:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-tabs == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-tabs lint --if-present
    - run: npm run -w components/o-tabs build --if-present
    - run: npm run -w components/o-tabs test --if-present
  test-components-o-teaser:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-teaser == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-teaser lint --if-present
    - run: npm run -w components/o-teaser build --if-present
    - run: npm run -w components/o-teaser test --if-present
  test-components-o-teaser-collection:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-teaser-collection == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-teaser-collection lint --if-present
    - run: npm run -w components/o-teaser-collection build --if-present
    - run: npm run -w components/o-teaser-collection test --if-present
  test-components-o-toggle:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-toggle == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-toggle lint --if-present
    - run: npm run -w components/o-toggle build --if-present
    - run: npm run -w components/o-toggle test --if-present
  test-components-o-tooltip:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-tooltip == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-tooltip lint --if-present
    - run: npm run -w components/o-tooltip build --if-present
    - run: npm run -w components/o-tooltip test --if-present
  test-components-o-top-banner:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-top-banner == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-top-banner lint --if-present
    - run: npm run -w components/o-top-banner build --if-present
    - run: npm run -w components/o-top-banner test --if-present
  test-components-o-topper:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-topper == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-topper lint --if-present
    - run: npm run -w components/o-topper build --if-present
    - run: npm run -w components/o-topper test --if-present
  test-components-o-typography:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-typography == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-typography lint --if-present
    - run: npm run -w components/o-typography build --if-present
    - run: npm run -w components/o-typography test --if-present
  test-components-o-video:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-video == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-video lint --if-present
    - run: npm run -w components/o-video build --if-present
    - run: npm run -w components/o-video test --if-present
  test-components-o-viewport:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-viewport == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-viewport lint --if-present
    - run: npm run -w components/o-viewport build --if-present
    - run: npm run -w components/o-viewport test --if-present
  test-components-o-visual-effects:
    needs: changes
    if: ${{ needs.changes.outputs.components-o-visual-effects == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w components/o-visual-effects lint --if-present
    - run: npm run -w components/o-visual-effects build --if-present
    - run: npm run -w components/o-visual-effects test --if-present
  test-libraries-ftdomdelegate:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-ftdomdelegate == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/ftdomdelegate lint --if-present
    - run: npm run -w libraries/ftdomdelegate build --if-present
    - run: npm run -w libraries/ftdomdelegate test --if-present
  test-libraries-math:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-math == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/math lint --if-present
    - run: npm run -w libraries/math build --if-present
    - run: npm run -w libraries/math test --if-present
  test-libraries-o-autoinit:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-o-autoinit == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/o-autoinit lint --if-present
    - run: npm run -w libraries/o-autoinit build --if-present
    - run: npm run -w libraries/o-autoinit test --if-present
  test-libraries-o-brand:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-o-brand == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/o-brand lint --if-present
    - run: npm run -w libraries/o-brand build --if-present
    - run: npm run -w libraries/o-brand test --if-present
  test-libraries-o-errors:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-o-errors == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/o-errors lint --if-present
    - run: npm run -w libraries/o-errors build --if-present
    - run: npm run -w libraries/o-errors test --if-present
  test-libraries-o-tracking:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-o-tracking == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/o-tracking lint --if-present
    - run: npm run -w libraries/o-tracking build --if-present
    - run: npm run -w libraries/o-tracking test --if-present
  test-libraries-o-utils:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-o-utils == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/o-utils lint --if-present
    - run: npm run -w libraries/o-utils build --if-present
    - run: npm run -w libraries/o-utils test --if-present
  test-libraries-sass-mq:
    needs: changes
    if: ${{ needs.changes.outputs.libraries-sass-mq == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w libraries/sass-mq lint --if-present
    - run: npm run -w libraries/sass-mq build --if-present
    - run: npm run -w libraries/sass-mq test --if-present
  test-presets-eslint-config-origami-component:
    needs: changes
    if: ${{ needs.changes.outputs.presets-eslint-config-origami-component == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w presets/eslint-config-origami-component lint --if-present
    - run: npm run -w presets/eslint-config-origami-component build --if-present
    - run: npm run -w presets/eslint-config-origami-component test --if-present
  test-presets-remark-preset-lint-origami-component:
    needs: changes
    if: ${{ needs.changes.outputs.presets-remark-preset-lint-origami-component == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w presets/remark-preset-lint-origami-component lint --if-present
    - run: npm run -w presets/remark-preset-lint-origami-component build --if-present
    - run: npm run -w presets/remark-preset-lint-origami-component test --if-present
  test-presets-stylelint-config-origami-component:
    needs: changes
    if: ${{ needs.changes.outputs.presets-stylelint-config-origami-component == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w presets/stylelint-config-origami-component lint --if-present
    - run: npm run -w presets/stylelint-config-origami-component build --if-present
    - run: npm run -w presets/stylelint-config-origami-component test --if-present
  test-tools-a11y:
    needs: changes
    if: ${{ needs.changes.outputs.tools-a11y == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/a11y lint --if-present
    - run: npm run -w tools/a11y build --if-present
    - run: npm run -w tools/a11y test --if-present
  test-tools-create-component:
    needs: changes
    if: ${{ needs.changes.outputs.tools-create-component == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/create-component lint --if-present
    - run: npm run -w tools/create-component build --if-present
    - run: npm run -w tools/create-component test --if-present
  test-tools-demo-build:
    needs: changes
    if: ${{ needs.changes.outputs.tools-demo-build == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/demo-build lint --if-present
    - run: npm run -w tools/demo-build build --if-present
    - run: npm run -w tools/demo-build test --if-present
  test-tools-origami-bower-safe-version-supervisor:
    needs: changes
    if: ${{ needs.changes.outputs.tools-origami-bower-safe-version-supervisor == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/origami-bower-safe-version-supervisor lint --if-present
    - run: npm run -w tools/origami-bower-safe-version-supervisor build --if-present
    - run: npm run -w tools/origami-bower-safe-version-supervisor test --if-present
  test-tools-origami-tools-helpers:
    needs: changes
    if: ${{ needs.changes.outputs.tools-origami-tools-helpers == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/origami-tools-helpers lint --if-present
    - run: npm run -w tools/origami-tools-helpers build --if-present
    - run: npm run -w tools/origami-tools-helpers test --if-present
  test-tools-sass-compilation:
    needs: changes
    if: ${{ needs.changes.outputs.tools-sass-compilation == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/sass-compilation lint --if-present
    - run: npm run -w tools/sass-compilation build --if-present
    - run: npm run -w tools/sass-compilation test --if-present
  test-tools-test-javascript:
    needs: changes
    if: ${{ needs.changes.outputs.tools-test-javascript == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/test-javascript lint --if-present
    - run: npm run -w tools/test-javascript build --if-present
    - run: npm run -w tools/test-javascript test --if-present
  test-tools-test-sass:
    needs: changes
    if: ${{ needs.changes.outputs.tools-test-sass == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/test-sass lint --if-present
    - run: npm run -w tools/test-sass build --if-present
    - run: npm run -w tools/test-sass test --if-present
  test-tools-validate-component-demo-html:
    needs: changes
    if: ${{ needs.changes.outputs.tools-validate-component-demo-html == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/validate-component-demo-html lint --if-present
    - run: npm run -w tools/validate-component-demo-html build --if-present
    - run: npm run -w tools/validate-component-demo-html test --if-present
  test-tools-verify-origami-json:
    needs: changes
    if: ${{ needs.changes.outputs.tools-verify-origami-json == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/verify-origami-json lint --if-present
    - run: npm run -w tools/verify-origami-json build --if-present
    - run: npm run -w tools/verify-origami-json test --if-present
  test-tools-verify-package-json:
    needs: changes
    if: ${{ needs.changes.outputs.tools-verify-package-json == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w tools/verify-package-json lint --if-present
    - run: npm run -w tools/verify-package-json build --if-present
    - run: npm run -w tools/verify-package-json test --if-present
  test-apps-storybook-addons-background:
    needs: changes
    if: ${{ needs.changes.outputs.apps-storybook-addons-background == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w apps/storybook/addons/background lint --if-present
    - run: npm run -w apps/storybook/addons/background build --if-present
    - run: npm run -w apps/storybook/addons/background test --if-present
  test-apps-storybook-addons-guidelines:
    needs: changes
    if: ${{ needs.changes.outputs.apps-storybook-addons-guidelines == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w apps/storybook/addons/guidelines lint --if-present
    - run: npm run -w apps/storybook/addons/guidelines build --if-present
    - run: npm run -w apps/storybook/addons/guidelines test --if-present
  test-apps-storybook-addons-html:
    needs: changes
    if: ${{ needs.changes.outputs.apps-storybook-addons-html == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w apps/storybook/addons/html lint --if-present
    - run: npm run -w apps/storybook/addons/html build --if-present
    - run: npm run -w apps/storybook/addons/html test --if-present
  test-apps-storybook-addons-markdown-tabs:
    needs: changes
    if: ${{ needs.changes.outputs.apps-storybook-addons-markdown-tabs == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w apps/storybook/addons/markdown-tabs lint --if-present
    - run: npm run -w apps/storybook/addons/markdown-tabs build --if-present
    - run: npm run -w apps/storybook/addons/markdown-tabs test --if-present
  test-apps-storybook:
    needs: changes
    if: ${{ needs.changes.outputs.apps-storybook == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w apps/storybook lint --if-present
    - run: npm run -w apps/storybook build --if-present
    - run: npm run -w apps/storybook test --if-present
  test-apps-website:
    needs: changes
    if: ${{ needs.changes.outputs.apps-website == 'true' }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: volta-cli/action@v4
    - uses: actions/cache@v2
      with:
        volta-version: "1.0.8"
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: npm run -w apps/website lint --if-present
    - run: npm run -w apps/website build --if-present
    - run: npm run -w apps/website test --if-present
