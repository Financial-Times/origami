name: percy components/ft-concept-button
concurrency:
  group: ${{ github.head_ref }}-${{ github.workflow }}-components/ft-concept-button
  cancel-in-progress: true

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  push:
    branches: main
    paths:
      - "components/ft-concept-button/package.json"
      - "components/ft-concept-button/package-lock.json"
      - "components/ft-concept-button/origami.json"
      - "components/ft-concept-button/main.js"
      - "components/ft-concept-button/main.scss"
      - "components/ft-concept-button/demos/**"
      - "components/ft-concept-button/src/**"

jobs:
  percy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: volta-cli/action@v1
    - uses: actions/cache@v2
      with:
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: node ./scripts/percy.js
      env:
        PERCY_TOKEN: ${{ secrets.PERCY_TOKEN_COMPONENTS_FT_CONCEPT_BUTTON }}
        WORKSPACE: components/ft-concept-button
