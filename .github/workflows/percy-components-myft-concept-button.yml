name: percy components/myft-concept-button
concurrency:
  group: ${{ github.head_ref }}-${{ github.workflow }}-components/myft-concept-button
  cancel-in-progress: true

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  push:
    branches: main
    paths:
      - "components/myft-concept-button/package.json"
      - "components/myft-concept-button/package-lock.json"
      - "components/myft-concept-button/origami.json"
      - "components/myft-concept-button/main.js"
      - "components/myft-concept-button/main.scss"
      - "components/myft-concept-button/demos/**"
      - "components/myft-concept-button/src/**"

jobs:
  percy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: volta-cli/action@v1
    - uses: actions/cache@v2
      with:
        path: ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    - run: npm ci
    - run: node ./scripts/percy.js
      env:
        PERCY_TOKEN: ${{ secrets.PERCY_TOKEN_COMPONENTS_MYFT_CONCEPT_BUTTON }}
        WORKSPACE: components/myft-concept-button
