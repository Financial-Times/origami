import <%- props.camelCase(props.name) %> from './src/js/<%- props.withoutPrefix(props.name) %>.js';

const constructAll = () => {
	<%- props.camelCase(props.name) %>.init();
	document.removeEventListener('o.DOMContentLoaded', constructAll);
};

if (typeof document !== 'undefined') {
	document.addEventListener('o.DOMContentLoaded', constructAll);
}

export default <%- props.camelCase(props.name) %>;
