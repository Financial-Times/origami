import <%- props.titleCase(props.withoutPrefix(props.name)) %> from './src/js/<%- props.withoutPrefix(props.name) %>.js';

const constructAll = () => {
	<%- props.titleCase(props.withoutPrefix(props.name)) %>.init();
	document.removeEventListener('o.DOMContentLoaded', constructAll);
};

if (typeof document !== 'undefined') {
	document.addEventListener('o.DOMContentLoaded', constructAll);
}

export default <%- props.titleCase(props.withoutPrefix(props.name)) %>;
