<% const componentName = props.camelCase(props.titleCase(props.withoutPrefix(props.name)))  -%>
import withHtml from 'origami-storybook-addon-html';
import {withDesign} from 'storybook-addon-designs';
import {ComponentStory, ComponentMeta} from '@storybook/react';
import {<%= componentName %>} from '../src/tsx/<%= props.withoutPrefix(props.name) %>';
import './<%= props.withoutPrefix(props.name) %>.scss';

export default {
	title: 'Components/<%= props.name %>',
	component: <%= componentName %>,
	decorators: [withDesign, withHtml],
	parameters: {},
	args: {},
} as ComponentMeta<typeof <%= componentName %>>;

const <%= componentName %>Story = args => <<%= componentName %> {...args} />;
export const Default<%= componentName %>: ComponentStory<typeof <%= componentName %>> = <%= componentName %>Story.bind(
	{}
);
