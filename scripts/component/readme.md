# component scripts

These are the scripts that are used by default in a component's `package.json`.

It's okay for them to live in the monorepo because `lint`, `build` and `test`
will only ever be run in the context of the monorepo.
