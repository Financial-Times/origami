language: node_js
node_js:
  - "0.10"
before_install:
  - npm install -g https://github.com/Financial-Times/origami-build-tools/tarball/node-0.10
  - origami-build-tools install
  # Commented out as PhantomJS doesn't support things like CustomEvents, so testing only works on Chrome
  # - npm install -g grunt-cli
  # - origami-build-tools build
script:
  - origami-build-tools test
  - origami-build-tools verify
