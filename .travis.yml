sudo: false
language: node_js
node_js:
  - 6
before_install:
  - npm install -g origami-build-tools@^7
  - obt install
script:
  - obt demo --demo-filter pa11y
  - obt verify
  - obt test
cache:
  directories:
    - node_modules
