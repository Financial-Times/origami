language: node_js
node_js:
  - "0.10"
before_install:
  - npm install -g https://github.com/Financial-Times/origami-build-tools/tarball/node-0.10
  - origami-build-tools install
  # Removed because test won't run on PhantomJS because it doesn't support things like CustomEvents 
  # - npm install -g karma
  # - npm install -g grunt-cli
script:
  - origami-build-tools test
