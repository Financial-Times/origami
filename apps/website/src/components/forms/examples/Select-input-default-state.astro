---
import Preview from '../../utils/Preview.astro'
import FigmaSbLinks from '../../utils/FigmaSbLinks.astro';

import * as SelectInputDefaultStateJSX from '../preview/Select-input-default-state';

const {brand} = Astro.props
const component = SelectInputDefaultStateJSX
const storybookId = `${brand}-o3-form--select-input`
---

<div>
	<Preview {component} />
	<FigmaSbLinks storybookId={storybookId} />
</div>

