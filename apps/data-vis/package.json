{
  "name": "@financial-times/data-vis",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-data": "node scripts/generate-data.js",
    "dev": "npm run generate-data && vite",
    "build": "npm run generate-data && vite build",
    "preview": "npm run generate-data && vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "peer-dependencies": {
    "@financial-times/o-colors": "^6.4.2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.1",
    "@tsconfig/svelte": "^3.0.0",
    "fs-jetpack": "^4.3.1",
    "svelte": "^3.49.0",
    "svelte-check": "^2.8.0",
    "svelte-preprocess": "^4.10.7",
    "tslib": "^2.4.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  },
  "dependencies": {
    "node-sass": "^7.0.1"
  }
}
