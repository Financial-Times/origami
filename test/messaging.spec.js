"use strict";

var expect = require('chai').expect;
var notification = require('../src/main.js');

describe("Messaging", function(){

	it("Should be able to show a message with the given content");

	it('Should show a message when an FT.Notification event is fired');

	it('Will hide the message if the user clicks the close button');

	it('Will hide message after 5 seconds if no other interaction');

});
